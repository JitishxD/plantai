# Add project-specific ProGuard rules here.
#
# The Android Gradle Plugin applies the default optimized rules first. Add
# keep/dontwarn rules in this file only when this app introduces a library or
# reflection-based model that needs them.
#
# For more details, see:
# https://developer.android.com/build/shrink-code

# If this app later exposes a JavaScript interface through a WebView, keep its
# public members, for example:
# -keepclassmembers class fully.qualified.JavaScriptInterface {
#     public *;
# }

# Preserve source line information for readable crash stack traces if required:
# -keepattributes SourceFile,LineNumberTable
# -renamesourcefileattribute SourceFile
